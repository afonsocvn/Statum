<nav>
  <a href="/">Statum</a>
  <a href="/countries">Países</a>
  <% if (currentUser) { %>
    <span>Ligado como <%= currentUser.username %></span>
    <form action="/logout" method="POST" style="display:inline">
      <button type="submit">Sair</button>
    </form>
  <% } else { %>
    <a href="/login">Entrar</a>
    <a href="/register">Registar</a>
  <% } %>
</nav>
